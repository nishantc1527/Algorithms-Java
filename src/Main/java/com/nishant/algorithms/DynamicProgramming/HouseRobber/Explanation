Given an array of integers, return the largest sum of all the elements not including consecutive ones.

Example:

Input([1,2,3,1])
Output(4)

Solution: solution is described the the description file in dynamic programming.